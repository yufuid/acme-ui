module.exports = {
  publicPath: "/acme-ui/",
  outputPath: "../../docs",
  /**
   * 网站的标题，默认是 package.json name TODO：
   */
  title: "Acme UI",
  /**
   * 网站的描述，会生成 mate 标签，默认是 package.json description TODO：
   */
  description: "Next Gen UI Design Language and Library",
  /**
   * 默认的 Docz 文档中提供编辑跳转到 github 的功能，这个用来配置调转过去的分支名 TODO：
   */
  editBranch: "main",
  /**
   * docz dev 环境的端口号配置，同样可配置 host
   */
  port: 3000,
};
