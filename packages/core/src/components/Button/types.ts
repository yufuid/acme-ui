export enum ButtonMode {
  CONTAINED = 'contained',
  OUTLINED = 'outlined',
  MINIMAL = 'minimal',
}

export enum ButtonColor {
  PRIMARY = 'primary',
  DANGER = 'danger',
}

export enum ButtonSize {
  DEFAULT = 'default',
  LARGE = 'large',
  SMALL = 'small',
}
