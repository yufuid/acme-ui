{{#if noteGroups}}
{{#each noteGroups}}

### {{title}}

{{#each notes}}
* {{#if commit.scope}}**{{commit.scope}}:** {{/if}}{{text}}

{{~> references root=@root shortHash=commit.shortHash hash=commit.hash references=commit.references}}
{{/each}}
{{/each}}

{{/if}}
