{{~#if subScope}} 
* {{subScope}}
{{#each commits}}
  * {{~#if subject}}{{~subject}}{{~else}}{{~header}}{{~/if}} {{~> references root=@root}}
{{/each}}
{{~else}}
{{~#each commits}}
* {{#if subject}}
  {{~subject}}
{{~else}}
  {{~header}}
{{~/if}}
{{~> references root=@root}}
{{/each}}
{{~/if}}